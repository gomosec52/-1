const $=s=>document.querySelector(s);async function api(url,opt={}){const r=await fetch(url,{headers:{'Content-Type':'application/json'},...opt});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Ошибка');return j}
async function load(){try{const d=await api('/api/admin/games');$('#adminGames').innerHTML=d.games.map(g=>`<div class="adminCard"><div><b>[Пак ${g.pack}] ${g.title}</b><br><span class="small">${g.genre} · ${g.size} · ${g.multiplayer}</span></div><div><button onclick='edit(${JSON.stringify(g).replaceAll("'","&#39;")})'>Редактировать</button><button onclick='del(${g.id})'>Удалить</button></div></div>`).join('')||'Нет игр'}catch(e){document.body.innerHTML='<main class="panel"><h1>Нет доступа</h1><p>Сначала впиши свой Site ID в ADMIN_IDS в .env и перезапусти сервер.</p><p><a href="/">На главную</a></p></main>'}}
function edit(g){const f=$('#gameForm');for(const [k,v] of Object.entries(g)){if(f[k])f[k].value=v||''}scrollTo(0,0)}
async function del(id){if(confirm('Удалить игру?')){await api('/api/admin/games/'+id,{method:'DELETE'});load()}}
window.edit=edit;window.del=del;$('#resetBtn').onclick=()=>$('#gameForm').reset();$('#gameForm').addEventListener('submit',async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target));if(data.id){await api('/api/admin/games/'+data.id,{method:'PUT',body:JSON.stringify(data)})}else{await api('/api/admin/games',{method:'POST',body:JSON.stringify(data)})}e.target.reset();load()});load();
